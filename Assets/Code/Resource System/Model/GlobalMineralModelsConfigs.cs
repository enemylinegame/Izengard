using UnityEngine;

namespace ResourceSystem
{ 
    public class GlobalMineralModelsConfigs : ScriptableObject
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
